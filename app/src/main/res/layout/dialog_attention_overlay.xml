<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5"
    android:padding="16dp">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="🎯 AI Diagnostic Visualization"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#2D3748" />

        <ImageButton
            android:id="@+id/btnClose"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close"
            android:contentDescription="Close"
            android:scaleType="centerInside" />

    </LinearLayout>

    <!-- Disease Info -->
    <TextView
        android:id="@+id/tvDiseaseInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Detected: Apple Scab (95.2% confidence)"
        android:textSize="16sp"
        android:textColor="#4A5568"
        android:gravity="center"
        android:layout_marginBottom="16dp"
        android:background="@drawable/modern_card_background"
        android:padding="12dp" />

    <!-- Full Screen Attention Overlay -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <ImageView
            android:id="@+id/imgAttentionOverlay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:background="@drawable/modern_card_background"
            android:contentDescription="Full-size AI attention overlay showing diagnostic focus areas" />

    </ScrollView>

    <!-- Instructions -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="💡 This shows where the AI focused when analyzing your plant. Red/yellow areas indicate the model's attention during disease detection."
        android:textSize="14sp"
        android:textColor="#666666"
        android:gravity="center"
        android:layout_marginTop="16dp"
        android:background="@drawable/modern_card_background"
        android:padding="12dp" />

</LinearLayout>
