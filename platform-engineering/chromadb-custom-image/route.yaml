apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: chromadb-custom-route
  namespace: your-namespace  # Replace with your actual namespace
  labels:
    app: chromadb-custom
spec:
  to:
    kind: Service
    name: chromadb-custom-service
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
